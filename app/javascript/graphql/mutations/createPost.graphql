mutation createPost($title: String, $content: String!) {
  createPost(title: $title, content: $content) {
    errors
    post {
      id
      isAuthor
      title
      content
      createdAt
      edited
      authorName
    }
  }
}
